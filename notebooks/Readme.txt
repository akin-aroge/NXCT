- To run:

	1. open "anaconda prompt"
	2. type "cd C:\Users\Yadvinder\Documents\Akin-Aroge\code\notebooks"
	3. type: "conda activate phd-im" and press enter
	4. type "jupyter lab"
	- this will open a browser tab with different "notbooks" for computing various metrics such as saturation, connectivity, etc
	5. open a desired notebook to perform the relevant computation
	6. To run the notbook: go to the file menu as click on run. In the drop down menu, click on "Run All Cells"
	7. Enter inputs as propmted
	8. result (if any) will be stored in the folder that contains the input image as specified.

